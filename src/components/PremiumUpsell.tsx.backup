import { Card } from './ui/card';
import { Button } from './ui/button';
import { Lock, ArrowRight } from 'lucide-react';

export const PremiumUpsell = () => {
  return (
    <section className="space-y-6">
      <div>
        <h2 className="text-xl font-semibold text-foreground">
          Conteúdo Avançado
        </h2>
        <p className="text-sm text-muted-foreground">
          Eleve sua jornada com módulos exclusivos
        </p>
      </div>

      <Card className="border-border p-6">
        <div className="flex flex-col gap-6 lg:flex-row lg:items-center">
          {/* Ícone */}
          <div className="flex h-16 w-16 shrink-0 items-center justify-center rounded-lg border border-border bg-accent">
            <Lock className="h-8 w-8 text-foreground" />
          </div>

          {/* Conteúdo */}
          <div className="flex-1 space-y-3">
            <div>
              <span className="mb-2 inline-block rounded-full bg-accent px-3 py-1 text-xs font-medium text-foreground">
                Premium
              </span>
              <h3 className="text-lg font-semibold text-foreground">
                A Deusa na Cama
              </h3>
              <p className="text-sm text-muted-foreground">
                Módulo de sedução avançada com técnicas que transformam momentos íntimos em experiências inesquecíveis.
              </p>
            </div>

            <div className="flex flex-wrap items-center gap-4 text-sm text-muted-foreground">
              <span>12 aulas</span>
              <span></span>
              <span>3h de conteúdo</span>
              <span></span>
              <span>Acesso vitalício</span>
            </div>
          </div>

          {/* Preço e CTA */}
          <div className="flex flex-col items-start gap-3 lg:items-end">
            <div className="text-right">
              <p className="text-sm text-muted-foreground line-through">997 MZN</p>
              <p className="text-2xl font-semibold text-foreground">597 MZN</p>
            </div>
            <Button className="gap-2">
              Desbloquear
              <ArrowRight className="h-4 w-4" />
            </Button>
          </div>
        </div>
      </Card>
    </section>
  );
};
